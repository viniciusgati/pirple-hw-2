
const server = require('./server')
const config = require('./config')
const routing = require('./routes')

console.log(routing)

// server.handlers = navigation.handlers
// server.routes = navigation.routes

// server.start.bind(server)(config)